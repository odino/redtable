name: Docker Compose Actions Workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker compose up -d
      - name: wait for things to be up (sorry 😬)
        run: sleep 10
      - name: Test
        run: make test
